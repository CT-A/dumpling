extends Shotgun

# Called when the node enters the scene tree for the first time.
func _ready():
	_path = "res://shotgun_2.tscn"
	damage = .513
	dmg_per_lvl = .513
	max_cd = 0.7
	rarity = 2
	super()

